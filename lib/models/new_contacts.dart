
import 'dart:async';

class NewContact{
  String? nom;
  String ? prenom;
  String? numero;
  String? mail;
  DateTime ? datenaissance;
  NewContact({
    required this.nom,
    required this.prenom,
    required this.numero,
    required this.datenaissance,
    required this.mail});
}